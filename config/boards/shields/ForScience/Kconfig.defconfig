if SHIELD_FORSCIENCE_LEFT

    config ZMK_KEYBOARD_NAME
        default "ForScience"

    config ZMK_SPLIT_BLE_ROLE_CENTRAL
        default y

endif

if SHIELD_FORSCIENCE_RIGHT

    config ZMK_KEYBOARD_NAME
        default "ForScience(Right)"

endif

if SHIELD_FORSCIENCE_LEFT || SHIELD_FORSCIENCE_RIGHT

    config ZMK_SPLIT
        default y

endif
